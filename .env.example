# --- Required: LLM Configuration ---
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=your_llm_api_key_here
MODEL_NAME=qwen-plus

# --- Optional: Server Port (Default 3000) ---
PORT=3000

# --- Optional: Web Search Tool (Bocha API) ---
BOCHA_API_KEY=your_bocha_api_key_here

# --- Optional: Send Mail Tool (SMTP) ---
MAIL_HOST=smtp.example.com
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USER=your_email@example.com
MAIL_PASS=your_email_password
MAIL_FROM=your_email@example.com

# --- Optional: MySQL (user / job CRUD via TypeORM) ---
# Defaults in src/app.module.ts: host localhost, db hello, user root

# --- Optional: Milvus (RAG vector store) ---
# Local Docker: milvus-standalone maps 19530:19530
MILVUS_ADDRESS=localhost:19530
MILVUS_COLLECTION=rag_docs
MILVUS_USER=
MILVUS_PASSWORD=
# Embedding model (must match your OPENAI_BASE_URL provider)
# OpenAI official: text-embedding-3-small / text-embedding-3-large
# DashScope: text-embedding-v3 / text-embedding-v4 (base URL must be dashscope compatible-mode)
EMBEDDING_MODEL=text-embedding-3-small
# Optional: set if auto-detect fails (OpenAI text-embedding-3-small default 1536)
# EMBEDDING_DIMENSIONS=1536
RAG_DOCS_PATH=docs/rag-sample
RAG_TOP_K=4
RAG_CHUNK_SIZE=800
RAG_CHUNK_OVERLAP=120
RAG_EMBED_BATCH=16
